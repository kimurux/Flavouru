<Window x:Class="Flavouru.Desktop.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Регистрация в Flavouru" Height="383" Width="500"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Регистрация в Flavouru" 
                   FontSize="20" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1">
            <Label Content="Имя пользователя:"/>
            <TextBox x:Name="txtUsername" Margin="0,0,0,10"/>
        </StackPanel>

        <StackPanel Grid.Row="2">
            <Label Content="Email:"/>
            <TextBox x:Name="txtEmail" Margin="0,0,0,10"/>
        </StackPanel>

        <StackPanel Grid.Row="3">
            <Label Content="Пароль:"/>
            <PasswordBox x:Name="txtPassword" Margin="0,0,0,10"/>
        </StackPanel>

        <StackPanel Grid.Row="4">
            <Label Content="Подтверждение пароля:"/>
            <PasswordBox x:Name="txtConfirmPassword" Margin="0,0,0,20"/>
        </StackPanel>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Зарегистрироваться" Width="150" Margin="0,0,10,0" Click="btnRegister_Click"/>
            <Button Content="Назад ко входу" Width="150" Click="btnBackToLogin_Click"/>
        </StackPanel>

        <TextBlock x:Name="txtError" 
                   Grid.Row="6" 
                   Foreground="Red" 
                   TextWrapping="Wrap" 
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Bottom"/>
    </Grid>
</Window>

